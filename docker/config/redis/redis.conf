bind 0.0.0.0
protected-mode no
port 6379
appendonly yes

# 컨테이너 제한(256M)보다 낮게 잡는다. AOF 버퍼와 클라이언트 출력 버퍼는 maxmemory 밖에서
# 따로 잡아먹기 때문에, 컨테이너 한도와 같은 값을 주면 OOM 킬을 당한다.
maxmemory 200mb
maxmemory-policy volatile-lru